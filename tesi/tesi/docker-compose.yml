services:
  db:
    image: postgres:latest
    ports:
      - "5432:5432"
    restart: always
    environment:
      POSTGRES_USER: postgres        # <--- Aggiunto (deve combaciare con application.properties)
      POSTGRES_PASSWORD: secret    # <--- Già presente
      POSTGRES_DB: postgres        # <--- Aggiunto (deve combaciare con l'URL)